# Terrain module 大型地理資產路徑（外接硬碟）。複製為 .env 或直接 export。
# 這些檔案數 GB、置於外接硬碟；程式碼絕不寫死路徑——一律經環境變數注入。
# 未掛載外接硬碟時：開發/測試用合成夾具自動替代；服務層以 try_open_default 降級運作。

# DTED 高程（O2.1）
MATSO_DTED_PATH=/Volumes/M200/Maps/TW_ALL.tif

# OpenStreetMap PBF（O2.2+ terrain_class / 土地利用；尚未使用）
MATSO_OSM_PBF_PATH=/Volumes/M200/Maps/taiwan.osm.pbf

# 道路網 graphml（O2.4+ 道路型 mobility；尚未使用）
MATSO_ROAD_GRAPH_PATH=/Volumes/M200/Maps/taiwan_drive.graphml

# hex grid 預計算 parquet 快取目錄（O2.2）。
# 建議放「本地」而非外接硬碟——如此預計算一次後，日常查詢不需接上外接硬碟即可運作。
# MATSO_HEX_CACHE_DIR=/Users/you/matso-hexcache
