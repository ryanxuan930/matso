# doctrine_red — 紅軍準則

紅軍（OPFOR）作戰準則、慣性與 TTP。供 `OPFOR_COMMANDER` 自主迴路檢索與逐字引用。

放：遲滯要領、攻擊/防禦準則、火力協調、指管慣性、撤退準則。
不放：藍軍內容（→ `doctrine_blue`）、裝備數據（→ `equipment_specs`）。

每份文件需 front-matter（`collection: doctrine_red`），段落給穩定錨點。
格式規範見上層 [`../README.md`](../README.md)；範例見 `red_delay_ops.md`。
