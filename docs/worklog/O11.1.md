---
task: O11.1
status: DONE
started: 2026-07-23T23:30+08:00
updated: 2026-07-23T23:55+08:00
agent: Opus 4.8
---

# O11.1 Faction COP context builder

## 目標摘要
為自主推演（SPEC_AUTONOMY §3.2）建「某陣營視角」的戰場快照 → LLM 指揮官 prompt 的輸入。純讀零 I/O：呼叫端傳入快照（熱狀態拷貝、單位靜態身分、**已霧化的敵情**、關係矩陣、目標、近期事件），本模組只投影成緊湊可序列化 dict + 文字 briefing。紅線：AI 不得看到未偵測敵（fog 在 `known_enemies` 注入端強制，本模組不放寬）。

## 計畫
- [x] 定錨熱狀態單位鍵（lat/lng/strength/health/armor_class/ammo_by_weapon）、faction 在 DB、intel 現況（sensors NoOp→用 ground-truth HOSTILE 首版）
- [x] `core/app/ai_loop/context.py`：`UnitMeta`、`build_faction_context`、`render_context_prompt`
- [x] 單元測：己方/敵方分流、霧化不洩漏、序列化、prompt 渲染、狀態導出
- [x] gates：ruff/mypy/pytest 綠

## 測試證據
- `uv run pytest core/tests/unit/test_faction_context.py -q` → 11 passed
- `uv run ruff check core/app/ai_loop/context.py …` → All checks passed
- `uv run mypy core/app/ai_loop/context.py` → Success

## 決策與陷阱
- **敵情可見性注入化**：build_faction_context 不讀 DB/Redis，`known_enemies` 由呼叫端霧化後注入 → 純函數可測、fog-ready。首版 wiring（O11.4）用 ground-truth HOSTILE（sensors NoOp）；真偵測上線換 IntelService。
- **關係由宣告陣營導出**（unit_meta），非由熱狀態存活單位 → 不洩漏「某敵陣營尚有存活單位」。
- 熱狀態無 faction/status 欄：faction 來自 DB（UnitMeta），status 由 strength/health 導出。

## 執行紀錄
- `23:20` 盤點：AI 大腦（client/replay、role_manager、G1–G6、run_opfor_turn）皆現成；缺 context builder。落單走 OrderService.submit；victory DSL 骨架在 scenario/triggers.py；場景有 victory_conditions 欄。
- `23:25` 熱狀態單位鍵取自 seed_combat_state；**faction 不在熱狀態**（DB TacticalUnit）；活 sim 用 NoOpSensorSystem → IntelContact 空 → 敵情可見性做成可注入策略（首版 ground-truth HOSTILE，fog-ready）。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| core/app/ai_loop/context.py | 新增 | build_faction_context（純函數）+ render_context_prompt |
| core/tests/unit/test_faction_context.py | 新增 | 霧化/分流/序列化/渲染測試 |

## 中斷續作指引
- **O11.1 完成**。下一步 → O11.2：run_opfor_turn→run_faction_turn（含 alias）+ LlmFactionDecider（讀 #54 Ollama 設定，用 context.render_context_prompt 產 user prompt + build_system_prompt 產 system prompt）；core 容器加裝 matso_ai+httpx 並 rebuild。
- **供 O11.2 用的介面**：`build_faction_context(...)` 回 dict；`render_context_prompt(ctx)` 回 briefing 文字。敵情可見性策略（EnemyVisibility）於 O11.4 wiring 決定。
