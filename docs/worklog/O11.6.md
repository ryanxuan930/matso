---
task: O11.6
status: DONE
started: 2026-07-24T02:35+08:00
updated: 2026-07-24T02:45+08:00
agent: Opus 4.8
---

# O11.6 決定性重播（RecordingClient/ReplayClient 接線）

## 目標摘要
把既有 ReplayClient/RecordingClient（ai/matso_ai/inference/client.py）接進 decider 工廠：自主場次
可錄一次（RecordingClient 包 Ollama）供 CI/air-gapped 以 ReplayClient 零網路重播，達決定性。golden
6 不含 AI，路徑不變。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| core/app/ai_loop/decider.py | 改 | build_llm_client（replay_dir/env MATSO_LLM_REPLAY_DIR→ReplayClient；record_dir/env MATSO_LLM_RECORD_DIR→RecordingClient 包真 client；皆無→OpenAICompatibleClient）；make_llm_faction_decider 透傳 |
| core/tests/unit/test_llm_faction_decider.py | 改 | +4 tests（client 選擇 ×3、record→replay 決定性 ×1） |

## 測試證據
- test_llm_faction_decider（15）→ passed
- **golden replay 6 → passed（AI 變更不觸 golden 路徑）**
- mypy 全量 191 → Success；ruff → All checks passed

## 決策與陷阱
- 決定性策略 env 驅動：core 容器設 `MATSO_LLM_REPLAY_DIR` 即整場走重播、零網路；`MATSO_LLM_RECORD_DIR` 錄 fixture。orchestrator 不需改（make_llm_faction_decider 讀 env）。
- record→replay 決定性靠 prompt_hash（messages+model+adapter）；同 context/role/mode → 同 hash → 同回應 → 同決策。
- 預設（無 env）行為不變 → 不需立即重建容器（下次重建自然帶入）。

## 中斷續作指引
- **O11.6 完成**。下一步 O11.7：前端自主主控台（設定：陣營指派/人格/目標/AI 模式 → PUT /autonomy；觀戰：COP+事件+AI 推理軌跡+護欄判決+目標進度；結果：勝負橫幅+AAR）。
- **前端可用 API**：PUT/GET/DELETE /api/v1/sessions/{id}/autonomy（O11.4）；SESSION_CONCLUDED 事件於 WS 事件流（勝負橫幅）；GUARDRAIL_INTERVENTION 事件（護欄判決）。
