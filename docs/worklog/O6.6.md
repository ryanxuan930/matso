---
task: O6.6
status: DONE
started: 2026-07-21T00:00+08:00
updated: 2026-07-21T00:00+08:00
agent: Opus 4.8
---

# O6.6 eval gate 進 CI（條件式）——M6 收尾

## 目標摘要
把 `matso_ai.evals.run`（§19.4 四門檻）接進 CI（用 fallback/replay mock，非真模型）。**案例庫空 →
schema-only + EVAL_CORPUS_EMPTY 警告（仍綠）**。真模型 eval 為手動觸發 workflow。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| .github/workflows/ci.yml | 修改 | python job 加「AI eval gate」step（`python -m matso_ai.evals.run`）|
| .github/workflows/ai-eval-manual.yml | 新增 | 真模型 eval 手動 workflow（workflow_dispatch，端點 input；未提供→fallback）|
| ai/tests/test_evals.py | 修改 | +CLI exit code 測試（範例→0、空庫→0）|

## 測試證據
- `python -m matso_ai.evals.run` → exit 0（3 範例 PASS）；`--cases-dir <空>` → exit 0（schema-only）
- 全套 589 passed；mypy 140 clean；ruff；schema-sync；workflows 皆 valid yaml

## 決策與陷阱
- **CI 用 fallback mock 非真模型**：CI 每次 push 只驗「管線正確 + 案例合法 + schema 通過」，不驗
  模型戰術品質（§19.4 條件式）。真模型 eval 成本高、需可達 vLLM 端點 → 獨立手動 workflow。
- **空案例庫→綠 + 警告**：eval 資料未到位時 CI 不擋（passed=True），但輸出 EVAL_CORPUS_EMPTY
  明示「綠燈≠戰術品質」。部署前最小案例集為 checklist 項（§19.4），非 CI 綠可替代。
- 真模型 responder 接線（RoleManager+client→run_evals responder）屬部署層；手動 workflow 已備
  端點 input 供日後注入。

## 中斷續作指引
- **本卡 DONE → M6（AI Phase 1）里程碑達成**。branch `feat/o6.6-eval-ci`（從 feat/o6.5-opfor-loop）。
- **M6 全鏈路（空 RAG/eval 下可運作）**：模式（§9.0 AI_OFF/BARE/FULL）→ client/RoleManager（O6.1）→
  護欄 Gateway（O6.2）→ RAG 空庫降級（O6.3）→ prompts+eval runner（O6.4）→ OPFOR 迴路（O6.5）→
  eval 進 CI（O6.6）。
- **部署層待接（非 M6）**：真 vLLM 端點、真 bge-m3、Qdrant 服務、kernel↔OPFOR 迴路接線、
  WargameSession.aiMode 欄位（O7.4 白軍控制台）、真模型 eval 手動跑。
