---
task: O11.2
status: DONE
started: 2026-07-24T00:10+08:00
updated: 2026-07-24T00:35+08:00
agent: Opus 4.8
---

# O11.2 陣營泛化 + LlmFactionDecider（接 Ollama）

## 目標摘要
把 AI 決策迴路從 OPFOR-only 泛化為任一陣營可用，並把 LLM 接成 `run_faction_turn` 的具體 decider。單一本機模型（Ollama）以**陣營中性指揮官人格**服務 N 陣營（身分由 briefing/context 注入），adapter="base" 定址 → 切換成本 0。

## 計畫
- [x] `run_opfor_turn`→加 `run_faction_turn` 別名（迴路本就與陣營無關）+ 匯出
- [x] `ai/matso_ai/roles.py`：新增陣營中性 `FACTION_COMMANDER` 角色 + `ai/prompts/FACTION_COMMANDER.md`
- [x] `core/app/ai_loop/decider.py`：`LlmFactionDecider`（build_system_prompt + render_context_prompt + OUTPUT_INSTRUCTION → LLM → `_extract_json`）+ `make_llm_faction_decider` 工廠
- [x] 容器打包（SPEC §8.3-A）：core/pyproject 加 `matso-ai` 依賴 + `ai/matso_ai/py.typed` marker + `uv lock`
- [x] 測試：_extract_json（純/圍欄/雜訊/非物件）、decide、中性 prompt、feedback、run_faction_turn 端到端
- [x] gates + commit

## 測試證據
- `uv run pytest core/tests/unit/test_llm_faction_decider.py`（11）+ context/ai/tests → 61 passed
- 迴歸（decider+context+precheck+adjudicator+combined）→ 64 passed
- `uv run mypy` 全量 → Success（186 files，+2 新檔）
- `uv run ruff check .` 全量 → All checks passed

## 決策與陷阱
- **O11.3 定錨**：run_precheck 讀 **DB** `current_lat/lng`；movement 每 tick **同步寫回 DB**（movement.py:185/205）→ 活 sim 中 precheck 看得到即時座標，G3 feasibility 正確。
- **fire_policy 走 payload dict**：EngagePayload 未宣告該欄但 pydantic 預設 extra=ignore，raw payload 保留 → 傳到 adjudicator。O11.3 橋接把 fire_policy 塞進 OrderRequest.payload。
- matso_ai 直接注入 LLMClient（非經 RoleManager）→ O11.6 錄放最單純。

## 執行紀錄
- `00:12` run_opfor_turn 已是陣營無關（只依賴注入 decider/context/mode）；加 `run_faction_turn = run_opfor_turn` 別名，保留 run_opfor_turn 供事件驅動 OPFOR。
- `00:14` OPFOR_COMMANDER.md 綁死「你是紅軍」→ 多陣營不可用。新建 FACTION_COMMANDER（+prompt），身分由 briefing 注入。roles 無測試鎖定數量，加角色安全。
- `00:16` LlmFactionDecider 直接吃 LLMClient（可注入 Ollama/Recording/Replay），不經 RoleManager，讓 O11.6 錄放路徑最單純。_extract_json 容忍圍欄/雜訊；失敗回 {}（G1 擋→fallback HOLD）。
- `00:18` 容器：core Dockerfile `uv sync --package matso-core`→ core 需宣告 matso-ai 依賴才會進映像（連帶 httpx）。加 py.typed 讓 core 跨套件 import matso_ai 通過 mypy strict。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| core/app/ai_loop/opfor.py | 改 | 加 `run_faction_turn` 別名 |
| core/app/ai_loop/__init__.py | 改 | 匯出 run_faction_turn |
| core/app/ai_loop/decider.py | 新增 | LlmFactionDecider + make_llm_faction_decider + _extract_json |
| ai/matso_ai/roles.py | 改 | 新增 FACTION_COMMANDER 角色（output_schema=opfor_decision） |
| ai/prompts/FACTION_COMMANDER.md | 新增 | 陣營中性指揮官 prompt |
| ai/matso_ai/py.typed | 新增 | 讓 core 跨套件 import matso_ai 型別可解析 |
| core/pyproject.toml | 改 | 加 matso-ai 依賴（進 core 容器映像 + httpx） |
| uv.lock | 改 | matso-core→matso-ai |
| core/tests/unit/test_llm_faction_decider.py | 新增 | 11 tests |

## 中斷續作指引
- **待辦**：跑全量 gates（背景 btd0fi369）→ 更新本檔 DONE → commit → 進 O11.3。
- **O11.4 提醒**：core 容器要 `docker compose build core && up -d core` 才會實際載入 matso-ai（本機 venv 已 editable，測試已綠）。
- **供 O11.3/O11.4 用**：`make_llm_faction_decider(base_url, model, api_key, mode)`；decider.decide(context)→opfor_decision dict；run_faction_turn(decider, gateway, mode=, context=, feasibility=, no_strike_hexes=)。
