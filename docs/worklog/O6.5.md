---
task: O6.5
status: DONE
started: 2026-07-21T00:00+08:00
updated: 2026-07-21T00:00+08:00
agent: Opus 4.8
---

# O6.5 OPFOR 自主迴路（事件驅動→產令→護欄→物理預檢→pending，模式感知）

## 目標摘要
SPEC_FULL §9.1/§9.2/§10：OPFOR 一個回合＝decider 產決策 → Guardrail Gateway G1–G6 → 不過附回饋
重試 ≤2 → 仍不過 doctrine fallback（空令）。模式感知：AI_OFF 迴路不啟動（紅軍由人操作＝傳統兵推）。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| core/app/ai_loop/opfor.py | 新增 | run_opfor_turn：require_ai_enabled + 重試迴圈 + Gateway + fallback；OpforDecider 協定（注入）+ AiTurnResult |
| core/app/ai_loop/__init__.py | 新增 | 匯出 |
| core/tests/unit/test_ai_loop.py | 新增 | AI_OFF 拒啟動 / 有效產令 / 重試後接受 / no-strike 耗盡→fallback / AI_BARE 剔引用 / G3 剔不可行 / 可重現（7 test）|

## 測試證據
- `uv run pytest core/tests/unit/test_ai_loop.py` → 7 passed
- 全套 587 passed；mypy 140 clean；ruff

## 決策與陷阱
- **AI 無繞過物理特權**：sanitized orders 已過 G3 物理預檢（注入 feasibility）；不可行者剔除。
- **decider 為注入介面**：core 不載入期綁 ai。真 decider＝RoleManager + build_system_prompt +
  ReplayClient（部署層接線）；本模組只依賴抽象 → 決定性可測。
- **重試回饋**：不過時把護欄 reason 串成 feedback 餵下一輪（§9.2 附錯誤回饋重生成）。
- **fallback＝不下令**：耗盡重試 → 空令（安全預設），上層記 AI_OUTPUT_REJECTED；G4 硬阻擋亦升白軍。
- **ai_mode 持久化欄位（WargameSession.aiMode）→ O7.4**：mode 目前為 resolve_ai_mode（session
  override 字串 + 設定預設）的 param；White Cell 局中調整 mode 屬白軍控制台（O7.4）。

## 中斷續作指引
- **本卡 DONE**。branch `feat/o6.5-opfor-loop`（從 feat/o6.4-prompts-evals）。
- **部署層接線（非本卡）**：kernel 事件 → 建 context → run_opfor_turn（注入 RoleManager-decider、
  TerrainGatewayAdapter feasibility、QdrantCitationVerifier、scenario no_strike）→ orders 落 pending
  （OrderService）→ intervention_events 寫 Ledger。
- **交接 O6.6**：eval gate 進 CI。
