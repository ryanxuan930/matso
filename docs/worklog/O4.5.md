---
task: O4.5
status: IN_PROGRESS
started: 2026-07-21T00:00+08:00
updated: 2026-07-21T00:00+08:00
agent: Opus 4.8
---

# O4.5 下令 UX（指令面板、precheck 顯示、pending/歷史）

## 目標摘要
下令 UX（SPEC §13.4）：選單位 → 指令面板（MOVE/ENGAGE…）→ 送出 → **裁決前物理預檢結果顯示**
（可達性/ETA/彈藥）→ pending + 歷史列表 + 取消。驗收：**下 MOVE/ENGAGE 令全流程可用**。
後端：orders 端點改**由 token 推導 issuer**（SPEC §12：不信任前端）+ 新增 GET 列表。

## 計畫（分階段綠燈 commit）
- [ ] 後端：OrderRequest 去 issuer_id；validate_order/submit 收 issuer_id 參數；orders 端點 auth 推導 issuer + GET 列表
- [ ] 後端：E2E stub gateway（env 閘，precheck feasible）——讓下令全流程可離線 E2E（無 terrain）
- [ ] 後端測試更新（_order_fakes token / api auth / service/validator issuer 參數）+ 契約
- [ ] 前端：useOrders store（submit/cancel/list）+ 指令面板 + precheck 顯示 + pending/歷史 + 選單位/選點
- [ ] Playwright：選單位 → MOVE/ENGAGE → precheck 顯示 → pending 出現 → 取消
- [ ] 全關卡綠 + worklog + PROGRESS + commit

## 執行紀錄
- 開工：讀 SPEC §13.4、O3.1 order pipeline（service/validator/precheck、issuer_id 權限）。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| (待填) | | |

## 測試證據
- (待填)

## 決策與陷阱
- **issuer 由 token 推導**（非 body）：SPEC §12 前端不可信。orders 端點加 bearerAuth，查
  SessionParticipant(user, session) → issuer_id；非參與者 → 403。前端因此不需帶 issuer。
- **E2E stub gateway**（env `MATSO_STUB_GATEWAY`）：precheck 回 feasible + 假 ETA——讓下令全
  流程於無 terrain 的 SQLite E2E 可跑（同 seed_dev_user 的測試 affordance，非改真物理引擎）。

## 中斷續作指引
- **下一步第一件事**：後端 OrderRequest 去 issuer_id + validate/submit 收參數。
- **目前卡點**：無。
