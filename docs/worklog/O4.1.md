---
task: O4.1
status: DONE
started: 2026-07-21T00:00+08:00
updated: 2026-07-21T14:00+08:00
agent: Opus 4.8
---

# O4.1 認證 + Lobby（login/JWT/refresh；後端 auth 端點 Argon2id+JWT）

## 目標摘要
M4 前端起點。全端：**後端 auth 端點**（Argon2id 密碼雜湊 + JWT 短效 access + refresh；
POST /auth/login、/refresh、/logout；bearer 保護的 get_current_user）+ **前端** login/lobby
頁（Nuxt 4 + Pinia auth store + route middleware + 由 core_api.yaml 生成型別）。驗收（SPEC §12
認證、§13.1 頁面）：Playwright 登入→lobby、錯誤密碼被拒、token refresh 流程。契約先行
（core_api.yaml auth schema）。faction-scope 後端強制（前端過濾不可信，SPEC §12）。

## 計畫（分階段綠燈 commit）
- [x] 契約先行：core_api.yaml /auth/login|/refresh|/logout|/me + LoginRequest/TokenPair/RefreshRequest/SessionSummary + bearerAuth + 新 error code
- [x] 後端 auth 模組：hashing（argon2-cffi）、tokens（PyJWT，注入時鐘）、service、schemas、errors、config（jwt_secret env）、api/auth router、get_current_user dep
- [x] 後端 lobby：GET /sessions（role/faction 過濾）+ POST /sessions（建局）
- [x] 後端測試：hashing/token/service 單元 + login/refresh/me/logout API + 錯誤密碼被拒 + lobby
- [x] 前端：Pinia auth store、login/lobby/index、middleware/auth.global、useApi（bearer + 401 自動 refresh）、runtimeConfig、openapi 型別生成
- [x] CORS middleware（前端跨來源）+ seed_dev_user 種子腳本
- [x] Playwright：auth.spec（5 測試）+ config；實跑 5 綠
- [x] 全關卡綠 + worklog + PROGRESS + commit

## 執行紀錄（附時間，由上而下追加）
- 開工：讀 SPEC §12/§13.1/§16.1、core_api.yaml、core/app、platform。O4.1 從 main（M0–M3）開，與 M5 平行。
- Commit 1（366f8a4）後端 auth+lobby：契約先行 → argon2/pyjwt → service/api → 56 測試綠。
- Commit 2（a08a160）前端 + E2E + CORS：Nuxt pages/store/composable → CORS → seed → Playwright。
- **E2E 除錯（三個真 bug）**：
  1. 埠衝突：3000/8000 被使用者其他專案（archery）占用 → Playwright reuseExistingServer 抓錯 app。
     改用 MATSO 專屬埠 8100/3100（同 mariadb 3307 慣例）。
  2. **cookie-ref 競態**：登入後 `access.value=token` 尚未寫回 cookie，另一次 useCookie 讀到舊值
     → 下個請求漏帶 Bearer → /auth/me 401「缺少 bearer」。修：token refs 記憶於 nuxtApp，全呼叫共用同一 ref。
  3. **水合競態**：Playwright 在 Vue 水合前點擊 → @submit.prevent 未附掛 → 觸發原生表單送出 →
     停在 /login 無錯誤。修：app.vue 加 data-hydrated 標記，測試 gotoHydrated 等待後才互動。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| contracts/core_api.yaml | 修改 | auth/lobby 端點 + schema + bearerAuth + 新 error code |
| core/app/auth/{hashing,tokens,service,schemas,__init__}.py | 新增 | Argon2id + JWT（注入時鐘）+ authenticate/refresh/current_user |
| core/app/lobby/{service,schemas,__init__}.py | 新增 | GET/POST sessions（角色/參與過濾；建局→director 參與者；種子 blake2b 導出）|
| core/app/api/{auth,lobby}.py + deps.py + __init__.py | 新增/修改 | routers + get_current_user/get_auth_service/get_lobby_service |
| core/app/{errors,config,main}.py | 修改 | auth error code + jwt/cors 設定（env 注入）+ CORS middleware + 啟動警告 |
| core/pyproject.toml | 修改 | argon2-cffi + pyjwt |
| ops/tools/seed_dev_user.py | 新增 | 種子使用者（sqlite 建表 / mariadb upsert）|
| platform/app/composables/useApi.ts | 新增 | apiFetch（bearer+auto-refresh）+ apiLogin + useAuthTokens（nuxtApp 記憶 refs）|
| platform/app/stores/auth.ts | 新增 | Pinia：login/fetchMe/logout |
| platform/app/pages/{login,lobby,index}.vue + middleware/auth.global.ts + app.vue | 新增/修改 | 頁面 + 路由守衛 + 水合標記 |
| platform/app/types/api.ts | 新增（生成）| openapi-typescript 由 core_api.yaml 生成（入 git）|
| platform/{nuxt.config.ts,package.json,playwright.config.ts} + e2e/auth.spec.ts | 新增/修改 | pinia/runtimeConfig + gen:api/test:e2e script + E2E |

## 測試證據
- `uv run pytest -m "not benchmark"` → **393 passed / 8 skipped / 4 deselected**；auth/lobby 近 100%；mypy(86)/ruff/schema-sync 綠
- `cd platform && npm run lint && npm run typecheck` → 綠
- `cd platform && npx playwright test` → **5 passed**（登入→lobby、錯誤密碼被拒、未登入導回、建局、access 過期自動 refresh）
- 手動瀏覽器驗證：login 頁渲染、登入後 lobby 顯示 commander（EXERCISE_DIRECTOR）

## 決策與陷阱
- **JWT/密碼用真實牆鐘（非 SimClock）**：認證屬執行期基礎設施，非模擬引擎——與 terrain_client
  用 time.monotonic 同理，不違反紅線。token 簽發時間以注入的 `now` 供測試決定性。
- **jwt_secret 絕不寫死**：env 注入（MATSO_JWT_SECRET）；未設時開發預設 + 啟動警告（同 CWA/DTED 慣例）。
- **faction-scope 後端強制**（SPEC §12）：前端過濾不可信；本卡先立 get_current_user + 角色，
  session/unit 的 faction 過濾隨各端點落地。

## 中斷續作指引（⚠ 每次停筆前必須是最新狀態）
- **本卡已 DONE**。全關卡綠 + Playwright 5 綠。branch `feat/o4.1-auth-lobby`（從 main 開），未推送。
- **交接給後續 M4 卡**：
  - lobby 只做 list+create；**join（加入 session）+ 完整生命週期**（scenario 載入、kernel 生成）
    屬 O7/O8。建局目前只建 WargameSession 列 + director 參與者、master_seed 由建局名 blake2b 導出。
  - **E2E 進 CI 是 O4.6**：本卡 Playwright 本機可跑（npm run test:e2e），未加進 .github/ci.yml。
    CI 若要跑需：安裝 chromium + 起 seeded core（sqlite）+ nuxt。
  - api.ts 為生成物但入 git；**改 core_api.yaml 後須 `npm run gen:api` 重生**。
  - 正式部署 MUST 設 JWT_SECRET（≥32 bytes）與 CORS_ORIGINS（實際前端來源）。
- **無 TOTP 2FA**：SPEC §12 提及 TOTP，本卡未做（totpSecret 欄已存在），留待後續安全強化卡。
