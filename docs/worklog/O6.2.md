---
task: O6.2
status: DONE
started: 2026-07-21T00:00+08:00
updated: 2026-07-21T00:00+08:00
agent: Opus 4.8
---

# O6.2 AI 運作模式 + Guardrail Gateway G1–G6

## 目標摘要
實作 SPEC_FULL §9.0 AI 模式（AI_OFF/AI_BARE/AI_FULL）與 §10 Guardrail Gateway（G1–G6，不可
bypass、攔截=Ledger 事件）。RAG/eval 目前皆空——G5 模式感知（AI_BARE/空庫時非空引用=捏造）。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| core/app/models/enums.py | 修改 | AiMode StrEnum（AI_OFF/AI_BARE/AI_FULL） |
| core/app/config.py | 修改 | ai_mode 預設（env MATSO_AI_MODE，AI_OFF）+ ai_adapter_quantized |
| core/app/errors.py | 修改 | AiDisabledError（AI_DISABLED, 409）+ GuardrailRejectedError（AI_OUTPUT_REJECTED, 422）|
| core/app/guardrails/schemas.py | 新增 | GuardrailFinding/Outcome、CitationVerifier 協定、NoRagCitationVerifier（空庫預設）、intervention_events（→Ledger）|
| core/app/guardrails/profiles.py | 新增 | GuardrailProfile + load_profile（yaml，quantized_override）|
| core/app/guardrails/guardrail_profiles.yaml | 新增 | cot_min_steps/citation_similarity/adapter_quantized |
| core/app/guardrails/gateway.py | 新增 | GuardrailGateway G1–G6（schema/CoT/物理/IHL-ROE/引用/量化）|
| core/app/guardrails/modes.py | 新增 | resolve_ai_mode + require_ai_enabled（AI_OFF→AiDisabledError）|
| ai/matso_ai/inference/role_manager.py | 修改 | RoleManager 收 mode，記入 AIInvocationLog.request（§9.0 規則 5，補 O6.1 缺口）|
| contracts/core_api.yaml | 修改 | Error enum +AI_DISABLED +AI_OUTPUT_REJECTED |
| pyproject.toml | 修改 | mypy override +yaml/jsonschema |
| core/tests/unit/test_guardrails.py | 新增 | 每個 G 必攔 + 模式感知 G5 + coa + 攔截→事件（16 test）|
| core/tests/unit/test_ai_modes.py | 新增 | resolve/require（5 test）|

## 測試證據
- `uv run pytest -m "not benchmark"` → **565 passed**（+21）；guardrails 覆蓋率 **98%**（≥95 達標）
- mypy 128 clean、ruff、schema-sync（未動 schema）、OpenAPI 契約驗證通過

## 決策與陷阱
- **Gateway 屬 core 不屬 ai**（SPEC §10）：AI 永不自我裁決合規；guardrails 用純輸入（AI 輸出 dict
  + schema + 注入的 physics/citation 檢查器），與 ai 子系統零耦合。
- **G5 模式感知**：AI_BARE 或 verifier.index_empty → 引用 MUST 空、非空即捏造剔除；AI_FULL 空庫
  自動退回 AI_BARE 語義。預設 NoRagCitationVerifier（空庫）正好對應「RAG 目前空」的現實。
- **retry ≤2→fallback（§9.2）** 屬 orchestration（O6.5 的生成迴圈），Gateway 只報 G1/G2 blocked。
- **G3/G4 no-strike 清單**是 Scenario 資產（O7.1）——Gateway 收注入的 no_strike_hexes，預設空。
- **ai_mode 持久化**：本卡只到 enum + 設定預設 + resolve/require；**per-session 欄位（WargameSession.aiMode
  + migration）留 O6.5**（session 驅動 AI 時才需讀），避免加未被讀取的欄。

## 中斷續作指引
- **本卡 DONE**。branch `feat/o6.2-guardrails`（從 feat/o6-ai-modes-spec）。
- **交接 O6.3（RAG）**：實作 CitationVerifier 真身（Qdrant 檢索 + 相似度）替換 NoRagCitationVerifier；
  空庫回 index_empty=True（自動降級已由 Gateway 處理）。
- **交接 O6.5**：加 WargameSession.aiMode 欄（prisma migration）；OPFOR 迴路入口 require_ai_enabled；
  生成→Gateway.evaluate→intervention_events 寫 Ledger→sanitized orders 走物理預檢→pending。
  G3 feasibility 注入 TerrainGatewayAdapter；G4 no_strike 由 scenario 載入。
