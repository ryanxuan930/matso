---
task: O11.8
status: DONE
started: 2026-07-24T03:10+08:00
updated: 2026-07-24T03:20+08:00
agent: Opus 4.8
---

# O11.8 韌性收尾（逾時/速率/守衛/觀測）

## 目標摘要
自主推演的異常保護：LLM 逾時 → fallback 不卡；AI 落單速率上限（防洗版）；runaway 累計守衛；
心跳下限；per-worker 觀測日誌。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| core/app/ai_loop/orders_bridge.py | 改 | submit_faction_orders 加 max_orders（預設 25）速率上限；超量截斷記 BridgeResult.capped + warning |
| core/app/ai_loop/worker.py | 改 | 心跳夾 _MIN_HEARTBEAT_S（5s，防緊迴圈）；累計落單 > _MAX_TOTAL_ORDERS（500）→ runaway 守衛停止；per-cycle 觀測日誌加 #cycle/累計/超量 |
| core/app/ai_loop/decider.py | 改 | build_llm_client 加 timeout（預設 60s）→ OpenAICompatibleClient；LLM 逾時拋錯 → worker fallback HOLD，不卡 worker 執行緒 |
| core/tests/unit/test_orders_bridge.py | 改 | +test_submit_rate_cap（10 令上限 3 → capped 7、僅處理 3） |

## 測試證據
- test_orders_bridge（15）+ test_ai_worker（8）→ 23 passed
- ruff/mypy（ai_loop）→ 綠

## 決策與陷阱
- LLM 逾時/失敗已由 worker 迴圈 try/except 吸收（→ 下一週期，等同 HOLD）；timeout 縮短只是讓卡住的呼叫更快釋放 worker 執行緒。
- 速率上限截斷「不靜默」：記 capped + warning（符合工程守則：有界即需可見）。
- runaway 守衛為異常保護（正常對抗遠不到 500 令）；心跳下限防誤設。

## 中斷續作指引
- **O11.8 完成 → O11 里程碑（自主推演 O11.1–O11.8）全數交付**。
- 剩餘可選強化：更豐富觀戰面板（AI 推理逐條/目標進度）、真偵測霧化（IntelService 換 ground_truth）、scenario relations 注入（取代預設全 HOSTILE）、動態熱掛載 ai_config（免重啟 runner）、移動真實化（見 PROGRESS Backlog）。
- **live 煙測建議**：新建 session（含雙陣營單位）→ /system-settings 設 AI_BARE + Ollama → 該 session /autonomy 勾雙陣營 → runner 起跑即自動對抗，COP 觀戰、勝負橫幅收場。
