---
task: O11.7
status: DONE
started: 2026-07-24T02:50+08:00
updated: 2026-07-24T03:05+08:00
agent: Opus 4.8
---

# O11.7 前端自主主控台（設定/觀戰/結果）

## 目標摘要
前端讓使用者一鍵指派 AI 陣營並觀戰到收場。設定＝per-session 指派哪些陣營由 AI 控制 + 任務目標 +
決策心跳（PUT /autonomy）；觀戰＝沿用 COP（AI 令進指令列、護欄干預/勝負進戰況事件）；結果＝COP 勝負橫幅。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| platform/app/pages/session/[id]/autonomy.vue | 新增 | 自主推演主控台：由 units 取陣營 → 勾選 AI 控制 + 任務 + 心跳 → PUT/DELETE /autonomy；運作說明 + COP 連結 |
| platform/app/pages/session/[id]/cop.vue | 改 | header 加「自主推演」入口（canControl）；victory 橫幅（串流見 SESSION_CONCLUDED → 顯勝方 + 看 AAR） |
| core/app/state/broadcaster.py | 改 | 事件 envelope flatten 加 winners（供前端勝負橫幅） |
| core/tests/integration/test_hot_state_redis.py | 改 | 修預存失敗：empty-diff 測試改用非心跳 tick（CLOCK 每 5 tick 送心跳屬預期，非 bug） |

## 測試證據
- 前端 lint + typecheck → 綠
- broadcaster 整合測試 8 passed（修好 CLOCK 心跳相關的預存失敗）
- ruff/mypy（broadcaster）→ 綠；容器重建 healthy

## 決策與陷阱
- 觀戰沿用既有 COP（不另造重畫）：AI 令進指令列、GUARDRAIL_INTERVENTION/SESSION_CONCLUDED 進戰況事件流。
- 勝負橫幅讀事件 envelope 的 winners（broadcaster 新增 flatten 欄）。
- **預存 bug 修復**：test_broadcaster_skips_empty_diff 用 tick=0（% 5 == 0）撞上 CLOCK 心跳→改 tick=1。此為 CLOCK 心跳功能新增後的 latent 失敗（整合測試不在 CI），非本次 AI 變更引入。

## 中斷續作指引
- **O11.7 完成**。下一步 O11.8：韌性（LLM 逾時 fallback、AI 指令速率上限、runaway 迴圈守衛、per-worker 觀測）。
- **後續（非首版）**：更豐富觀戰面板（AI 推理軌跡逐條、目標進度條）——目前推理入 Ledger/事件流可見。
