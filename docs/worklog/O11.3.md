---
task: O11.3
status: DONE
started: 2026-07-24T00:40+08:00
updated: 2026-07-24T00:55+08:00
agent: Opus 4.8
---

# O11.3 護欄 G3 feasibility + 指令橋接 VALIDATED

## 目標摘要
把 AI 產出的 tactical_order 接上物理世界：G3 護欄以 run_precheck 判物理可行性、橋接把令經 OrderService.submit 落成 VALIDATED（issuer＝AI 陣營 participant），與人類同入口，Kernel 照常 drain 執行。紅線：AI 不繞物理、不寫熱狀態。

## 檔案異動
| 檔案 | 動作 | 說明 |
|------|------|------|
| core/app/ai_loop/orders_bridge.py | 新增 | tactical_order_to_request（MOVE/ENGAGE；HOLD→skip）；PrecheckFeasibility（G3，只查物理不查權限，直建 ValidatedOrder→run_precheck）；submit_faction_orders（逐筆 submit，捕 MatsoError 歸 rejected，回 BridgeResult） |
| core/tests/unit/test_orders_bridge.py | 新增 | 14 tests（映射/可行性/落單 VALIDATED/權限拒/HOLD skip） |

## 測試證據
- `uv run pytest core/tests/unit/test_orders_bridge.py -q` → 14 passed
- ruff / mypy（orders_bridge.py）→ 綠

## 決策與陷阱
- G3 feasibility **只查物理**（直建 ValidatedOrder→run_precheck），不查下令權限——權限在 submit 端（validate_order 需 issuer 為 SessionParticipant）。
- fire_policy 走 payload dict（EngagePayload extra=ignore，raw payload 保留 → adjudicator 讀）。
- MOVE 預設 mobility_profile="FOOT"（同前端 COP 預設）。**首版簡化**：未依單位型別/載具選 profile（見 PROGRESS Backlog；與使用者回報的地形/耗損議題相關）。
- HOLD 不在 OrderType → 映射回 None（原地待命，不落單）。

## 中斷續作指引
- **O11.3 完成**。下一步 O11.4：sim_runtime 每 AI 陣營起 async 固定心跳 worker（取 COP 快照→LlmFactionDecider→run_faction_turn[G1-G6 + PrecheckFeasibility 注入 feasibility]→submit_faction_orders）；建 AI participant（issuer）；core 容器 rebuild 裝 matso-ai。
- **供 O11.4 用**：`PrecheckFeasibility(db, session_id, gateway, relations)`→注入 run_faction_turn 的 feasibility；`submit_faction_orders(db, session_id, orders, issuer_id=, gateway=, relations=, tick_source=)`→BridgeResult。
