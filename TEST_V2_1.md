# V2.1 全系統測試報告

**測試日期**：2026-07-30 ~ 07-31
**對象**：`ops/compose` 全套服務（core / frontend / mariadb:3307 / redis / qdrant / terrain / tileserver / weather / comms）
**方法**：靜態關卡 + **活體端到端**（只透過對外 HTTP API 操作正在跑的服務）+ 瀏覽器實測

---

## 0. 為什麼要做活體測試（而不是「pytest 綠了就好」）

這個 repo 反覆出現同一類缺陷：**存得進去、讀得回來、測試全綠、實際沒效果**。

單元測試餵的是自己組的假物件，整合測試用的是 in-process 的假 kernel——兩者都測不到
「跑在 container 裡的那條 runner 有沒有真的把單位移動」。本輪因此新增
[`ops/tools/live_system_check.py`](ops/tools/live_system_check.py)：每一條檢查都問
**「觀測得到的事實有沒有照物理改變」**，不問「有沒有回 200」。回 200 而世界沒動，在這裡算失敗。

```bash
uv run python ops/tools/live_system_check.py
```

本輪 12 條檢查抓到 **4 個真缺陷**，其中 3 個是單元測試全綠、畫面看起來也正常的靜默失效。

---

## 1. 靜態關卡

| 關卡 | 結果 |
|---|---|
| `uv run pytest -q -m "not benchmark"` | **2154 passed**, 8 skipped |
| `uv run ruff check .` | All checks passed |
| `uv run mypy` | 271 files, no issues |
| `npx @bufbuild/buf lint` | clean |
| `uv run python ops/tools/schema_sync_check.py` | 24 tables / 238 columns 一致 |
| `cd platform && npm run lint` | clean |
| `cd platform && npm run typecheck` | clean |
| `cd platform && npm test` | **48 passed** |

---

## 2. 活體端到端檢查（12/12 通過）

想定為檢查專用，即時產生：藍軍步兵排/裝甲連/155 砲兵連/油罐排 vs 紅軍步兵排/裝甲連，
沿同一條緯線佈署於**雲林平原**（履帶車走得通、通視不被擋），距離刻意安排成
「步兵 400 m 在步槍射程內、火砲到目標 10 km 在 155 射程內、裝甲往東 3 km 驗位移與油耗」。

| # | 檢查 | 實測結果 |
|---|---|---|
| C1 | 開局：想定落地成真的世界 | 6 單位就位、間距 400 m、編裝與 `fixed` 旗標一致 |
| C2 | 心跳：runner 真的在推進 | tick 5 → 10（120× 時間壓縮，設計如此） |
| C3 | 移動：MOVE 令真的把單位搬過去 | 行進 2996 m、最終偏差 **0 m**、指令 COMPLETED |
| C4 | 油料：滿箱是整個編成的量 | 對照組滿箱 **7600**（=1900×4）；走 1 km 燒 **18.0**（=4.5×4）；徒步單位 0 |
| C5 | 直射交戰：造成傷亡並消耗彈藥 | 戰力 100 → 51.3、彈藥 **900 → 810**（30 人 × 3 發） |
| C6 | 壓制：被打的單位壓制度上升 | 壓制度 0.07；**藍軍視角讀不到紅軍壓制度**（不外洩） |
| C7 | 間瞄火力：10 km 外真的打得到 | 10.4 km 射擊、砲彈 200 → 192、目標戰力 51.3 → 46.8 |
| C8 | 戰爭迷霧：只看得到自己＋接觸 | 藍 units=4／contacts=2；紅 units=2／contacts=3；無交叉洩漏 |
| C9 | 權限：偽造/缺席 token 被擋 | 401/403；統裁時間控制（PAUSE/RESUME）正常 |
| C10 | 檢查點：存得下可回滾的快照 | 有快照、帶 `state_hash` |
| C11 | AAR：帳本／重播／統計對得上 | 帳本 45 筆／17 影格；6/6 單位有基準座標 |
| C12 | 收場：不誤判 | 紅軍尚存 → 正確維持 ACTIVE |

### 地形引擎的旁證

第一版想定設在**中央山脈**（121.0E/23.7N），結果 MOVE 預覽回 `terrain_impassable`、
400 m 的步槍交戰被 28 m 的地形擋掉視線。**地形引擎判得完全正確**，錯的是想定選錯地方——
這反而是通視與可通行性真的在運作的最強證據。已改設於雲林平原（A* 有繞路，`terrain_routed=true`）。

---

## 3. 本輪抓到並修正的缺陷

### 缺陷 1 — 單武器交戰的彈藥沒寫回 DB（靜默失效）

**症狀**：`GET /units/{id}/weapons`（COP 單位卡與選彈畫面讀的就是它）**永遠顯示滿彈**。
一個排打光了子彈，畫面上還是 900 發；sim 一重啟熱狀態重新播種，彈匣還會自己補滿。

**成因**：聯合兵種（≥2 武器）與火力任務兩條路徑早就都持久化到 `EquipmentInstance` 了，
唯獨**最常見的那條**——一個單位一種武器（步槍排）——只扣熱狀態的純量 `ammo`，
既不動 `ammo_by_weapon` 也不寫 DB。聚合（營級以上）路徑同樣缺這一筆。

**影響**：指揮官據此決定要不要補給。這是會導致錯誤決策的錯誤。

**修正**：`adjudication/adjudicator.py` 抽出 `_persist_ammo` / `_fired_instance_id`，
單武器與聚合兩條路徑一併補上。判不出是哪一件裝備時**回 None 而不猜**——
猜錯會扣到別把槍的彈藥，那比不扣還糟（數字是錯的但看起來很合理）。

**活體驗證**：C5 由 `900 → 900` 變為 `900 → 810`。

### 缺陷 2 — 壓制衰減與工事工時寫死「1 tick = 1 分鐘」

**症狀**：整個 WP-C1 壓制/姿態子系統形同不存在。

**成因**：`POSTURE_TICKS` 的註解就寫著「1 tick = 1 分鐘」，但 `tick_rate_ms` 是**想定可調的**
（schema 允許 100 ms 以上），而移動、補給、耗損、整補**全都有除以它**。
官方 demo 與使用者的「玉山行動-0801」都寫 `tick_rate_ms: 1000`（1 tick ＝ 1 模擬秒）：

| | 應為 | 實際（1 秒/tick） |
|---|---|---|
| 掘壕（DUG_IN） | 4 小時 | **4 分鐘** |
| 準備陣地（DEFENSE） | 30 分鐘 | **30 秒** |
| 壓制完全恢復 | 約 13 分鐘 | **約 13 秒** |

工事變成一個幾乎免費的按鈕；壓制在下一次齊射前就散光。

**修正**：常數改以**分鐘**為單位，依該局 tick 長度換算（`posture_ticks()` /
`decay_suppression(..., tick_rate_ms)`）。所有預設值等同 `tick_rate_ms=60000`，
既有呼叫端與 golden 錄影位元不變。

**活體驗證**：C6 由「壓制度恆為 0」變為可觀測到 0.05–0.07。
⚠ 壓制窗口只有數秒，**必須高頻輪詢**（工具用 0.15 秒）；1.5 秒間隔會整段錯過，
然後看起來像「這個功能沒生效」——誤判成沒生效比逾時更糟。

### 缺陷 3 — 惰性滿油只填一台車的油

**症狀**：裝甲單位「怎麼開沒多遠就拋錨了」，很容易被誤判成地形或編裝設定問題。

**成因**：`load_unit_fuel` 的容量與油耗都乘了 `quantity`，**初始油量沒乘**。
4 輛 MBT 的連隊開局只有 1900（應 7600）卻以 18/km 消耗 → 續航 **105 km 而非 420 km**。
畫面上完全看不出異常（油量是個沒有基準的數字）。

**修正**：`remaining = capacity * qty`。

**活體驗證**：C4 對照組滿箱 **7600**、走 1 km 燒 **18.0**（=4.5×4）。

### 缺陷 4 — 刪除進行中的推演會 500

**症狀**：按「刪除」跳錯誤、再按一次就成功——看起來像隨機故障。

**成因**：刪一局進行中的推演會和它自己的 runner 搶列：偵測 sweep 每 tick 都在改寫
`IntelContact`，MariaDB 丟 1020「Record has changed since last read」。

**修正**：API 層先設收場旗標讓 runner 停下（O11.5 既有機制）、service 層對這個
暫時性錯誤重試（5 次 × 1 秒）。刪除失敗會把旗標清掉——否則使用者會留下一局
**再也不會前進**的推演，而畫面上完全看不出原因。

### 資料修復 — 「示範想定」開不了局

DB 裡的「示範想定」存的是 `victory_conditions[0].condition.type: eliminate`，
而 `eliminate` **從來不是合法的 condition type**。它出現在 lobby 的想定下拉裡、
點下去直接 422。WP-B6 已修好檔案端的 `tutorial-platoon`，但 DB 這一筆是更早存進去的。

已就地修正為 `{type: faction_eliminated, faction: RED}`（意圖無歧義），並掃描確認
DB 中其餘想定皆可正常開局。修正後實測開局成功。

---

## 4. 本輪新增功能

### 座標查詢（併入座標小工具）

支援三種格式，解析順序 MGRS → 度分秒 → 十進位度（最寬鬆的放最後，否則
`51QTG1234567890` 裡的數字會先被當成十進位度吃掉，得到一個**看起來像成功**的錯誤點）。

| 輸入 | 判讀 |
|---|---|
| `24.0972, 120.1705` | 依十進位度判讀 → 24.09720, 120.17050 |
| `24°05'50"N 120°10'14"E` | 依度分秒判讀 → 24.09722, 120.17056 |
| `51QTG1234567890` | 依MGRS判讀 → 24.09721, 120.17049 |
| `這不是座標` | 明確拒絕並列出支援格式 |
| `999, 999` | 十進位度超出範圍（緯度 ±90、經度 ±180）；讀到 999, 999 |

**MGRS 分帶提醒**：`51QTG…` 與 `51RTG…` 解析到同一個點（100 km 方格代號加東北距已經定位，
分帶字母只是粗略消歧，函式庫不報錯）。但對抄座標的人來說，字母不符通常代表**抄錯了一碼**
——而那一碼的代價可能是火力落在別的地方。故不擋，但把正規寫法一起顯示出來讓人核對。

### 小工具置頂

開啟小工具時自動 `focusWidget`。實測：開啟座標查詢得到 `z=21`，高於信文 17 / 地圖編輯 15 /
指令 14 / 戰況 13 / 單位 12 / 圖層 11。

### 單位屬性編輯（番號／兵科／編制級別／人數／戰力）

過去單位卡只有「編裝編輯（武器/彈藥）」，改不了人數與番號。後端其實一直有
`PATCH /sessions/{id}/units/{uid}`，但（一）沒有 UI、（二）**契約裡沒有這條路徑**、
（三）改不了人數與戰力。三件事一併補齊：

- **契約先行**：補上 `editUnit` 路徑與 schema，並從 `test_contract_conformance` 的
  已知漂移清單刪除該條（那份清單只能變短）。
- **改了要真的生效**：人數/戰力/建制數在開局時被播種進熱狀態，此後裁決層只讀熱狀態。
  只寫 DB 的話畫面上人數變了、實際打起來還是舊編制。新增 `state/live_unit.py` 命令通道
  （與 `live_ammo`/`live_position` 同紀律：單一寫入者，API 只 RPUSH、sim 迴圈 drain）。
- **導出量明確拒絕**：`health_status` 由戰力比導出、裁決層每次命中都會覆寫，送了回 422
  並指出正確做法是改 `current_strength`——靜默忽略會讓統裁以為自己補了血。
  `unit_level` 回 `restart_required`（聚合裁決門檻是 runner 起跑才讀的）。

**活體驗證**：改人數 42→55、戰力 100→60、番號改名 →
Redis 熱狀態同步為 `strength=60` / `platform_count=55`，
COP 單位卡、單位清單與戰況事件三處皆即時更新。

---

## 5. 已知限制（非缺陷，但要知道）

- **玉山行動-0801 的單位沒有座標**：該想定的 ORBAT 未填 `lat`/`lng`，故 36 個單位在
  COP 上沒有圖標。這是想定資料而非系統缺陷，但一個沒有座標的單位等於不存在於戰場——
  建議在想定編輯器補上部署位置。
- **`tick_rate_ms: 1000` 不是好的預設**：那是 schema 預設值被原封帶過來的。
  以 1 秒/tick 跑，一個排一 tick 只走 1.4 公尺。官方 `tutorial-platoon` 已改為 60000。
- **WEGO / IGO_UGO 尚未實作**：選了仍以即時制執行（UI 標籤已標示「未實作」）。
- **感測/通聯/後勤子系統**：`sim_runtime` 目前只接 movement + engagement，
  其餘為 NoOp（見既有 backlog）。

---

## 6. 重跑方式

```bash
cd ops/compose && docker compose up -d --wait
```

```bash
uv run python ops/tools/live_system_check.py
```
