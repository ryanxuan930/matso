# MATSO Phase 1 拓撲（SPEC_FULL §20.1）
# AI 節點不在 compose 內：獨立主機跑 vLLM，core 以 OPENAI_BASE_URL 指向。
# 尚未加入：terrain/weather（M2/M5）、tileserver、prometheus/grafana（M0 後續卡）。
name: matso

services:
  mariadb:
    image: mariadb:11
    environment:
      MARIADB_DATABASE: matso
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-matso_dev_root}
    volumes: [mariadb-data:/var/lib/mysql]
    # 主機 3306 常被其他 MariaDB 占用（本機環境即是），對外一律用 3307
    ports: ["3307:3306"]
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      retries: 5

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      retries: 5

  qdrant:
    image: qdrant/qdrant:latest
    ports: ["6333:6333"]
    volumes: [qdrant-data:/qdrant/storage]
    healthcheck:
      test: ["CMD-SHELL", "bash -c ':> /dev/tcp/127.0.0.1/6333' || exit 1"]
      interval: 10s
      retries: 5

  core:
    build:
      context: ../..
      dockerfile: core/Dockerfile
    environment:
      DATABASE_URL: mysql://root:${DB_ROOT_PASSWORD:-matso_dev_root}@mariadb:3306/matso
      REDIS_URL: redis://redis:6379/0
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-http://host.docker.internal:8080/v1}
    ports: ["8000:8000"]
    depends_on:
      mariadb: { condition: service_healthy }
      redis: { condition: service_healthy }

  frontend:
    build:
      context: ../../platform
    environment:
      NUXT_PUBLIC_API_BASE: http://core:8000
    ports: ["3000:3000"]
    depends_on:
      core: { condition: service_healthy }

volumes:
  mariadb-data:
  qdrant-data:
