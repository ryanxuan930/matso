# ⚠ 本專案的前端**一律以 container 執行**（CLAUDE.md 環境速查；使用者裁示 2026-07-29）：
#     cd ops/compose && docker compose up -d --build frontend
#   容器的設定來自 `ops/compose/.env` + docker-compose.yml 的 frontend.environment，**與本檔無關**。
#
# 本檔只是「萬一真的要用 `npm run dev` 直跑」時的範本（複製成 `platform/.env`；.env 已 gitignore）。
# 之所以留著：dev server 讀不到 compose 的環境變數，沒有這個檔會**沒有街道與線上底圖**——
# 這正是過去底圖反覆消失的成因之一，故保留說明以免有人再踩。

# Core REST（瀏覽器直連；預設值即此，通常不必改）。
NUXT_PUBLIC_API_BASE=http://localhost:8000

# 離線向量瓦片（tileserver-gl）。空字串＝只有背景+經緯網格，**底圖選單不會有「街道」**。
# 啟動：cd ops/compose && docker compose --profile tiles up -d tileserver（對外 8081）。
NUXT_PUBLIC_TILE_URL=http://localhost:8081

# 線上底圖總開關：NLSC 官方圖資（通用電子地圖/正射影像/1-5000 地形圖）+ Google/Esri。
# **需外網，非 air-gapped**，
# 故預設關；ops/compose/.env 目前為開發方便設為 1，本機 npm run dev 要同樣設定才會出現。
NUXT_PUBLIC_ONLINE_BASEMAPS=1

# 選用：自建衛星影像 XYZ（軍方/商用影像接入點）、額外自訂底圖來源（JSON 陣列）。
# NUXT_PUBLIC_SATELLITE_URL=https://<影像服務>/{z}/{x}/{y}.png
# NUXT_PUBLIC_BASEMAPS=
