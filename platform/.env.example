# 本機 `npm run dev` 的前端設定範本 —— 複製成 `platform/.env` 即可（.env 已 gitignore）。
# compose 跑前端時不需要這個檔（環境變數由 docker-compose.yml 的 frontend.environment 注入）；
# 但**直接 npm run dev 時 compose 的環境變數不會生效**，沒有本檔街道底圖會整個不出現。

# Core REST（瀏覽器直連；預設值即此，通常不必改）。
NUXT_PUBLIC_API_BASE=http://localhost:8000

# 離線向量瓦片（tileserver-gl）。空字串＝只有背景+經緯網格，**底圖選單不會有「街道」**。
# 啟動：cd ops/compose && docker compose --profile tiles up -d tileserver（對外 8081）。
NUXT_PUBLIC_TILE_URL=http://localhost:8081

# 選用：衛星 / 自訂底圖來源（軍方影像接入點）、線上底圖（需外網，非 air-gapped）。
# NUXT_PUBLIC_SATELLITE_URL=
# NUXT_PUBLIC_BASEMAPS=
# NUXT_PUBLIC_ONLINE_BASEMAPS=true
